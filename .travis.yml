language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ctong/docker-react -f Dockerfile.dev .

script:
  - docker run ctong/docker-react npm run test -- -- coverage 
